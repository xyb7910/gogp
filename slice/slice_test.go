package slice

import "testing"

func TestSlice(t *testing.T) {
	testCases := []struct {
		name string
		// 输入的参数

	}{
		{},
	}
	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}
